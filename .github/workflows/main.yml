name: Build & Test 🧪

on:
  push:
    branches:
      - main

env:
  DOTNET_VERSION: '7.0.x'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET 📦
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Install dependencies 📂
        run: dotnet restore PDFUABox.sln

      - name: Build 🧱
        run: dotnet build PDFUABox.sln --configuration Release --no-restore

      - name: Test 🧪
        run: dotnet test PDFUABox.sln --configuration Release --no-build
